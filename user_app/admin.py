from django.contrib import admin

# Register your models here.

# For user app, we'll not create any ModelForms, rather we'll use Django's built-in forms.
# As we don't have any ModelForms, we don't need to register anything into the database.
# Therefore, leaving this file empty.
